# Encore — Karaoke Studio
# Works on Python 3.10 – 3.13. CPU by default; CUDA/Metal are used automatically
# when available (see requirements-gpu.txt for an explicit CUDA build).

# GUI — the meta package, because the stage needs QtMultimedia from Addons
PySide6>=6.8.0

# Stem separation
torch>=2.7.0
torchaudio>=2.7.0
demucs>=4.0.0

# Real-time audio I/O and file handling
soundfile>=0.12.1
sounddevice>=0.4.6
numpy>=1.24.0,<3.0.0
scipy>=1.11.0

# YouTube search and download
yt-dlp>=2025.11.12

# Decoding downloads into something soundfile can read
ffmpeg-python>=0.2.0

# Packaging
pyinstaller>=6.0.0
